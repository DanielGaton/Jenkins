pipeline {
    agent {any }
    stages {
        stage('build') {
            steps {
                echo'construyendo'
            }
          
        }
         stage('deploy') {
            steps {
                echo'deplegao'
            }
          
        }
    }
}
